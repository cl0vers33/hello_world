# 进度报告一

> 课程：移动应用软件开发实训　第1课（第3周，4学时）
> 主题：移动应用开发概述与 AI 增强开发环境
> 姓名：成思辰　学号：（见学籍系统）　班级：（Flutter 线）
> GitHub 仓库：https://github.com/cl0vers33/hello_world （分支 master，lecture1 目录提交作业文档）

---

## 一、任务理解

本课两件事：一是看清移动开发四条技术路线（原生、Web、混合、跨平台）与 Flutter 在其中的位置；二是把一套"Flutter SDK + 平台工具链 + AI 编程工具（TraeWork/TraeCode）"的开发环境搭起来、跑起来、留痕起来。最终交付一个能在 Web 与 Android 模拟器都运行的 hello_world 仓库，外加一份覆盖十节的进度报告。

## 二、环境与工具

| 项 | 值 |
|----|----|
| 操作系统 | Microsoft Windows 11（版本 10.0.26200.9445，25H2） |
| Flutter 版本 | 3.47.2（Channel stable，gitee 镜像源） |
| Dart 版本 | 3.13.2 |
| 运行目标 | Chrome 152（Web）、sdk gphone64 x86 64（Android 模拟器）、Windows 桌面 |
| Android SDK | 36.0.0 |
| Visual Studio | 生成工具 2022 17.14.41（September 2026） |
| TraeWork 版本 | TraeCode（AI 原生 IDE），内置对话智能体与终端 |
| 国内镜像 | `PUB_HOSTED_URL=https://pub.flutter-io.cn`；`FLUTTER_STORAGE_BASE_URL=https://storage.flutter-io.cn` |

## 三、过程记录（时间线）

| 时间 | 动作 | 结果 |
|------|------|------|
| 课时前 | 用 PowerShell 脚本提取 `mapp-guide-1.docx` 与 `mapp-homework-1.doc` 文本 | 得到指南全文与作业单页全文 |
| 课时开始 | `flutter --version` | 3.47.2 正常输出，PATH 生效 |
| 课时开始 | `flutter doctor` | 7 项全绿 |
| 步骤一 | `flutter create hello_world` → `cd hello_world` | 项目骨架生成，含 lib/main.dart、android/ios/web/windows 等目录 |
| 步骤一 | 为 main.dart 添加逐行中文注释（AI 辅助，本人核对） | 完成；顺带修正模板文件两处类名缺失 |
| 步骤一 | `git init -b master`；配置全局 user.name/user.email | 仓库初始化，分支 master |
| 步骤一 | 首次提交 `功能：flutter create 创建 hello_world 项目` | commit 5b1a961（提交信息后统一改为中文，哈希为重写后值） |
| 步骤二 | `flutter build web --release` | 构建成功，输出 build/web |
| 步骤二 | 启动本地 HTTP 服务，浏览器打开 http://localhost:8080 | 计数器示例正常显示，截图 docs/web_run.png |
| 步骤二 | `flutter emulators --launch flutter_emulator` | 模拟器启动，`adb devices` 显示 emulator-5554 |
| 步骤二 | `flutter run -d emulator-5554`（首次） | **失败**：Gradle 下载 SSL 握手失败 |
| 步骤二 | 修复 SSL：改 Gradle wrapper URL 为腾讯镜像，settings.gradle.kts 前置阿里云 Maven | — |
| 步骤二 | `flutter run -d emulator-5554`（第二次） | **失败**：项目路径含中文，AGP 拒绝 |
| 步骤二 | 修复路径：`android/gradle.properties` 加 `android.overridePathCheck=true` | — |
| 步骤二 | `flutter run -d emulator-5554`（第三次） | **成功**：应用在模拟器启动；后统一改为桌面整屏截图 docs/emulator_run.png |
| 步骤二 | 第二次提交 `功能：在 Web 端与模拟器运行并保存截图` | commit 6846e7e |
| 步骤三 | 重写 README.md（项目名、运行方式、两端截图） | — |
| 步骤三 | 第三次提交 `文档：添加 README 运行说明与截图` | commit dc6165c |
| 课后整理 | `flutter doctor` 复检发现 Visual Studio 项 [!]：缺少 Windows 10 SDK | 经本人在 UAC 确认后，由 VS Installer 加装 Windows 10 SDK 10.0.19041 |
| 课后整理 | 再次 `flutter doctor` | **7 项全绿，No issues found!**（4 devices），另开真实终端窗口截图 docs/flutter_doctor.png |
| 课后整理 | GitHub 网页端创建空仓库（本人操作，账号 cl0vers33） | https://github.com/cl0vers33/hello_world |
| 课后整理 | `git remote add origin` + 首次 `git push -u origin master` | 前 3 次提交推送成功 |
| 课后整理 | 第四次提交 `文档：添加 lecture1 作业任务记录与进度报告` | commit 9759488 |
| 课后整理 | 中途多次小步提交（更新证据、完善报告、AI 记录等） | 共 6 条（7883001、5062358、2e9b0be、7c3ecd7、66c47f5、467fee7） |
| 课后整理 | 应本人要求精简历史，将上述 6 条合并为一条 | `文档：完善 lecture1 作业（证据截图整屏重截、AI 边界记录、progress.md、进度报告完善）`，最终 5 条提交 |

## 四、关键代码

摘自 [lib/main.dart](../lib/main.dart) 第 3-5 行与第 7-36 行，逐行解释如下：

```dart
void main() {
  runApp(const MyApp());
}
```

- 第 3 行：`main` 是 Dart 程序入口，操作系统启动应用时第一个调用的函数。
- 第 4 行：`runApp` 把根组件 `MyApp` 挂载到屏幕上；`const` 表示 `MyApp` 是编译时常量，Flutter 据此做组件复用与性能优化。
- 第 5 行：函数体结束。

```dart
class MyApp extends StatelessWidget {
  const MyApp({super.key});
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Flutter Demo',
      theme: ThemeData(
        colorScheme: ColorScheme.fromSeed(seedColor: Colors.deepPurple),
      ),
      home: const MyHomePage(title: 'Flutter Demo Home Page'),
    );
  }
}
```

- `class MyApp extends StatelessWidget`：`MyApp` 是无状态组件，界面不随交互改变；继承 Flutter 的 `StatelessWidget`。
- `const MyApp({super.key})`：构造函数，`super.key` 把 key 透传给父类，用于组件复用与更新识别。
- `Widget build(BuildContext context)`：`build` 方法返回组件树，描述界面长什么样；`BuildContext` 是组件在树中位置的句柄。
- `MaterialApp(...)`：Material 风格应用容器，提供主题、路由等基础能力。
- `title`：应用在任务切换器中显示的标题。
- `ThemeData(colorScheme: ColorScheme.fromSeed(seedColor: Colors.deepPurple))`：基于种子色 `deepPurple` 生成完整配色方案。**AI 辅助生成**：原模板文件此处类名缺失，AI 起草注释时补全为 `ColorScheme.fromSeed`。
- `home: const MyHomePage(title: 'Flutter Demo Home Page')`：指定首页为 `MyHomePage`，传入标题字符串。

> 是否 AI 生成：是（注释由 AI 辅助起草，本人逐行核对；代码逻辑保持 `flutter create` 原样，仅修正两处类名缺失）。

## 五、检查点结果

### 检查点1：flutter doctor 全绿

`flutter doctor` 输出 7 项 [√]、`No issues found!`，详见 [docs/flutter_doctor_output.txt](../docs/flutter_doctor_output.txt)。复检时曾出现 Visual Studio 项 [!]（缺 Windows 10 SDK），经 VS Installer 补装后转绿。截图见第八节图1。

### 检查点2：HelloWorld 在 Web 端运行

`flutter build web --release` 构建成功，本地服务在 Chrome 打开 http://localhost:8080，计数器示例正常显示（紫色 AppBar + 居中计数 + 右下角 FAB）。截图指引：见第八节图2。

### 检查点3：HelloWorld 在 Android 模拟器运行

`flutter run -d emulator-5554` 成功启动应用，模拟器显示与 Web 端一致的计数器界面。截图指引：见第八节图3。

### 检查点4：仓库首次提交与多次提交

仓库地址：https://github.com/cl0vers33/hello_world （Public，可直接访问）

提交记录（master 分支，按时间倒序；提交信息均为中文）：

```
最新提交  文档：完善 lecture1 作业（证据截图整屏重截、AI 边界记录、progress.md、进度报告完善）
9759488  文档：添加 lecture1 作业任务记录与进度报告
dc6165c  文档：添加 README 运行说明与截图
6846e7e  功能：在 Web 端与模拟器运行并保存截图
5b1a961  功能：flutter create 创建 hello_world 项目
```

满足"3 次以上提交"要求，提交说明均为中文语义化格式，仓库不含 build/ 等垃圾文件（.gitignore 已排除）。

注：历史中原有 6 条"完善报告/更新证据/AI 记录"类小步提交，已应本人要求合并为一条，最终共 5 条提交。

### 检查点5：口头解释 runApp 与 Widget

- `runApp`：Flutter 框架的入口挂载函数，把传入的根组件（这里是 `MyApp`）挂到屏幕上，启动组件树的渲染与调度。等价于"把根 Widget 作为整棵 UI 树的根"。
- `Widget`：Flutter 描述界面的基本单位。一切界面元素（`Text`、`Button`、`Scaffold`、`MaterialApp`）都是 Widget；Widget 是不可变的配置描述，真正的渲染由对应的 `Element` 与 `RenderObject` 完成。可以理解为"Widget 是图纸，Element 是按图纸搭的实体，RenderObject 是真正量尺寸画像素的那一层"。

## 六、问题与调试

> 详细记录见 [任务2_flutter_doctor问题定位与修复记录.md](任务2_flutter_doctor问题定位与修复记录.md)

### 问题1：Gradle 下载 SSL 证书验证失败

- **现象**：`flutter run -d emulator-5554` 时报 `javax.net.ssl.SSLHandshakeException: PKIX path building failed`。
- **定位**：Gradle wrapper 从 `services.gradle.org` 下载 Gradle 9.3.1 发行版，Java truststore 不识别其证书链。
- **解决**：`gradle-wrapper.properties` 改 `distributionUrl` 为 `https://mirrors.cloud.tencent.com/gradle/gradle-9.3.1-all.zip`；`settings.gradle.kts` 的 `repositories` 前置阿里云 Maven 镜像。
- **验证**：再次运行，Gradle 成功下载，进入编译阶段。

### 问题2：项目路径含非 ASCII 字符

- **现象**：SSL 修复后构建仍失败，报 `Your project path contains non-ASCII characters`。
- **定位**：项目路径 `...\作业\移动应用开发实践\课程1\hello_world` 含中文，Android Gradle Plugin 拒绝。
- **解决**：`android/gradle.properties` 追加 `android.overridePathCheck=true`（按报错提示的官方开关）。
- **验证**：再次运行，构建通过，应用在模拟器启动。

### 问题3：flutter doctor 的 Visual Studio 项报 [!]（缺 Windows 10 SDK）

- **现象**：课后复检 `flutter doctor`，Visual Studio 项变为 `[!] Unable to locate a Windows 10 SDK`，`C:\Program Files (x86)\Windows Kits` 目录不存在。
- **定位**：本机只装了 VS 2022 生成工具的 C++ 编译组件，未勾选任何 Windows SDK 版本；Flutter 桌面构建需要 Windows SDK 提供系统 API 头文件与库。
- **解决**：由本人在 UAC 弹窗确认后，通过 VS Installer 为 BuildTools 加装组件 `Microsoft.VisualStudio.Component.Windows10SDK.19041`（系统级修改，AI 仅给出命令与组件名，执行由本人完成）。
- **验证**：安装完成后重跑 `flutter doctor`，Visual Studio 项转 [√]，整体 7 项全绿。

## 七、AI使用记录

| 用途 | 指令摘要 | 输出 | 验证方式 |
|------|---------|------|----------|
| 提取 Word 文档文本 | 写 PowerShell 脚本读取 .docx/.doc | 得到指南与作业单页全文 | 人工对照原文 |
| 为 main.dart 加中文注释 | "为 main.dart 添加逐行中文注释" | 注释版 main.dart | `flutter build web` 编译通过 + 两端运行通过 |
| 定位 Gradle SSL 报错 | 贴报错栈，问"怎么修" | 改 Gradle wrapper URL + 阿里云镜像 | 再次 `flutter run` 成功 |
| 定位路径中文报错 | 贴报错，问"怎么修" | 加 `android.overridePathCheck=true` | 再次 `flutter run` 成功 |
| 定位 VS 缺 SDK 警告 | 贴 doctor 的 [!] 输出问原因 | 给出组件 ID 与 VS Installer 命令 | 本人执行安装后 doctor 转绿 |
| GitHub 建仓与推送 | "创建仓库并推送" | remote 添加与 push 命令 | 本人网页建仓，push 成功且页面可访问 |
| 提交信息改为中文 | "把 commit 中的标注改成中文" | 重写 7 条历史并强制推送 | 本人确认强制推送，远端 log 显示中文信息 |
| 起草 README 与各说明文档 | 按指南要求起草 | 多份 .md 文档 | 人工核对内容与截图一致 |
| 起草进度报告 | 按模板十节起草 | 本文件 | 人工核对后保留 |

## 八、证据截图

| 图号 | 文件 | 一句话说明 |
|------|------|-----------|
| 1 | [docs/flutter_doctor.png](../docs/flutter_doctor.png) | flutter doctor 7 项全绿，PowerShell 终端最大化整屏截图（输出文本见 [flutter_doctor_output.txt](../docs/flutter_doctor_output.txt)） |
| 2 | [docs/web_run.png](../docs/web_run.png) | Chrome 全屏运行 hello_world（localhost:8080）整屏截图，显示计数器示例 |
| 3 | [docs/emulator_run.png](../docs/emulator_run.png) | 桌面整屏截图：Android 模拟器窗口居中运行 hello_world，应用正常启动 |
| 4 | [docs/github_repo.png](../docs/github_repo.png) | Chrome 全屏打开 GitHub 仓库提交历史页整屏截图，含全部中文提交记录 |

注：以上 4 张证据图均于 2026-09-24 重新截取，统一为"在电脑上打开对应页面/窗口后的整屏画面"。

## 九、自评（对照 6.1~6.3 逐项）

### 6.1 案例复现

- [x] 完整复现 hello_world 并多端运行（Web + Android 模拟器）
- [x] 检查点全部通过（doctor 全绿、Web 运行、模拟器运行、仓库 3 次提交、口头解释 runApp/Widget）
- [x] Git 提交规范（语义化 commit message，无 build/ 等垃圾文件，.gitignore 已排除）
- [x] 进度报告复现记录完整（见第三节）

### 6.2 自主实践基本要求

| 序 | 任务 | 完成 | 证据 |
|----|------|------|------|
| 1 | TraeWork 辅助安装过程记录（指令、结果、验证三要素齐全） | ✓ | [任务1](任务1_TraeWork辅助安装过程记录.md) |
| 2 | flutter doctor 问题定位与修复记录 | ✓ | [任务2](任务2_flutter_doctor问题定位与修复记录.md)（含 2 个真实问题） |
| 3 | 手工安装原理说明（SDK、工具链、PATH） | ✓ | [任务3](任务3_手工安装原理说明.md) |
| 4 | HelloWorld 代码逐行中文注释 | ✓ | [任务4](任务4_HelloWorld代码逐行中文注释.md)；[main.dart](../lib/main.dart) |
| 5 | AI 使用边界说明（何时人工介入） | ✓ | [任务5](任务5_AI使用边界说明.md) |

### 6.3 独立研究任务（选做）

本次未完成选做任务，留待课后补做（计划选任务1：flutter config 与国内镜像机制实验）。

## 十、下一步计划

1. **补做独立研究任务**：优先做任务1（flutter config 与国内镜像机制实验），配置前后各下载一个包并对比耗时，用实验数据说话。
2. **真机测试**：用 USB 连接 Android 手机，开启 USB 调试，跑一次 `flutter run -d <设备ID>`，对比真机与模拟器的启动与交互流畅度（对应 6.3 任务3）。
3. **Windows 桌面端实跑**：SDK 已补齐，执行 `flutter run -d windows` 验证桌面目标，完成三端闭环。
4. **第5课预习**：Widget 树是后续主线，提前读《Flutter 实战》第1章与官方文档"Widgets"一节。
